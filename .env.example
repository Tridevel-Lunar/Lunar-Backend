# Copy to backend/.env (never commit)

CORS_ORIGINS=http://localhost:3000

# PostgreSQL
# Docker compose: set automatically via compose (compose.dev.yaml)
# Local backend + Docker postgres:
DATABASE_URL=postgresql://lunar:lunar@localhost:5432/lunar

# LAIKA (when implemented)
# GEMINI_API_KEY=
